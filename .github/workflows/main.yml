name: RDP-4GB-STABLE-FINAL
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: 1. Setup RDP
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1 -Force
          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: 2. Enable 4GB Virtual GPU
        run: |
          # Install Mesa (built-in to runner environment)
          choco install mesa-dist-win -y --no-progress || exit 0
          
          # Registry Hack for 4GB VRAM
          $regPath = "HKLM:\SOFTWARE\Microsoft\DirectX"
          if (!(Test-Path $regPath)) { New-Item $regPath -Force }
          New-ItemProperty -Path $regPath -Name "DedicatedVideoMemory" -Value 4294967296 -PropertyType QWORD -Force
          Write-Host "Virtual 4GB GPU Configured."

      - name: 3. Create User & Connect Tailscale
        shell: pwsh
        run: |
          # 1. Create User
          $pass = "P@ssw0rd4GB!"
          $sec = ConvertTo-SecureString $pass -AsPlainText -Force
          if (!(Get-LocalUser -Name "RDP" -ErrorAction SilentlyContinue)) {
            New-LocalUser -Name "RDP" -Password $sec -AccountNeverExpires
            Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          }

          # 2. Install Tailscale
          choco install tailscale -y --no-progress
          
          # 3. Refresh Path and Start
          $env:Path += ";C:\Program Files\Tailscale"
          
          # Use 'continue-on-error' logic manually
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-gpu-box --accept-routes || Write-Host "Tailscale warning ignored."
          
          # 4. Capture IP (Wait 10 seconds for network to stabilize)
          Start-Sleep -Seconds 10
          $ip = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          echo "IP=$ip" >> $env:GITHUB_ENV
          echo "PASS=$pass" >> $env:GITHUB_ENV

      - name: 4. Final Connection Info
        run: |
          Write-Host "=============================="
          Write-Host "IP Address: $env:IP"
          Write-Host "Username: RDP"
          Write-Host "Password: $env:PASS"
          Write-Host "=============================="
          while($true) { Start-Sleep 300 }
